apiVersion: v1
kind: Service
metadata:
  name: the-project-svc
spec:
  type: NodePort
  selector:
    app: the-project
  ports:
    - name: http
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 3000